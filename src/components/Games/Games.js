import React from "react"
import styled from "styled-components"

// Simple horizontally scrolling container for ticker games.
export const Games = styled.div`
    display: flex;
    flex-direction: row;
    overflow: auto;
`

